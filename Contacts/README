The PhoneDialer application contains some buttons which can be used to write a number and to call the number. Also contains a button which sends an intent to ContactsManager application (to main activity). The ContactsManager activity once opened can fill some fields required for a contact and some other additional fields. Pressing the SAVE button an insert intent is send in order to add the data in a contacts entry. 